const assert = require('chai');
const circularNumbers = require('../modules/circularNumbers');

describe('circularNumbers(position, limit)', () => {
  it('limit : 10, position 7 to 8 ', () => {
    



  });
});